name: Deploy to DigitalOcean

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      OP_ENVIRONMENT_ID: m5yflcyljim7b6fgc6hcjssca4

    steps:
      - name: Configure 1Password
        uses: 1password/load-secrets-action/configure@v2
        with:
          service-account-token: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}

      - name: Install 1Password CLI beta
        uses: 1password/install-cli-action@v2
        with:
          version: latest-beta

      - name: Load deployment secrets from 1Password Environment
        run: |
          op run --environment "$OP_ENVIRONMENT_ID" -- /bin/sh -c '
            printf "DO_HOST=%s\n" "$DO_HOST" >> "$GITHUB_ENV"
            printf "DO_USERNAME=%s\n" "$DO_USERNAME" >> "$GITHUB_ENV"
            printf "DO_SSH_KEY_B64=%s\n" "$DO_SSH_KEY_B64" >> "$GITHUB_ENV"
          '

      - name: Write SSH private key
        run: |
          printf "%s" "$DO_SSH_KEY_B64" | tr -d "[:space:]" | base64 --decode > /tmp/do_deploy_key
          chmod 600 /tmp/do_deploy_key

      - name: Render production environment file from 1Password Environment
        run: |
          python3 - <<'PYENV' > /tmp/base-env.json
          import json, os
          print(json.dumps(dict(os.environ)))
          PYENV

          op run --environment "$OP_ENVIRONMENT_ID" -- python3 - <<'PYENV' > /tmp/zentra.env.production
          import json, os
          from pathlib import Path

          base = json.loads(Path('/tmp/base-env.json').read_text())
          exclude = {
              'OP_SERVICE_ACCOUNT_TOKEN',
              'OP_ENVIRONMENT_ID',
              'DO_HOST',
              'DO_USERNAME',
              'DO_SSH_KEY_B64',
          }

          def quote(value: str) -> str:
              return "'" + value.replace("'", "'"'"'") + "'"

          for key in sorted(os.environ):
              value = os.environ[key]
              if key in exclude or key.startswith('GITHUB_'):
                  continue
              if base.get(key) != value:
                  print(f"{key}={quote(value)}")
          PYENV

          {
            printf 'APP_ENV_FILE_B64<<__ENV__
'
            base64 < /tmp/zentra.env.production | tr -d '
'
            printf '
__ENV__
'
          } >> "$GITHUB_ENV"

      - name: Deploy on DigitalOcean Droplet
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ env.DO_HOST }}
          username: ${{ env.DO_USERNAME }}
          key_path: /tmp/do_deploy_key
          envs: APP_ENV_FILE_B64
          script: |
            set -euo pipefail

            APP_ROOT="${{ secrets.DO_APP_ROOT }}"
            if [ -z "$APP_ROOT" ]; then
              APP_ROOT="/home/${{ env.DO_USERNAME }}/apps/zentra"
            fi

            BACKEND_DIR="$APP_ROOT/zentra-backend"

            mkdir -p "$APP_ROOT"

            if [ ! -d "$BACKEND_DIR/.git" ]; then
              git clone https://github.com/josedan10/finance-bot.git "$BACKEND_DIR"
            fi

            cd "$BACKEND_DIR"
            git fetch origin master
            git checkout master
            git pull --ff-only origin master

            cd "$BACKEND_DIR"

            if [ ! -f "$BACKEND_DIR/.env.production" ]; then
              printf "%s" "$APP_ENV_FILE_B64" | base64 --decode > "$BACKEND_DIR/.env.production"
              chmod 600 "$BACKEND_DIR/.env.production"
            fi

            chmod +x scripts/deploy-production.sh
            ./scripts/deploy-production.sh
